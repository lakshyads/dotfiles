# ~/.zsh_plugins.txt
# Antidote plugin bundle. Kept intentionally minimal; each plugin earns its
# place by solving a specific problem. Add more as needed.
#
# Load order matters: zsh-syntax-highlighting MUST load last (per its docs).
# zsh-autosuggestions and zsh-completions are order-flexible but listed before.

# Command autosuggestions based on history (press the right arrow to accept)
zsh-users/zsh-autosuggestions

# Extra completion definitions for common tools (git, docker, kubectl, etc.)
zsh-users/zsh-completions

# Syntax highlighting as you type (valid commands green, invalid red).
# IMPORTANT: must be the LAST plugin loaded, otherwise other plugins can
# unbind its widget hooks. See: https://github.com/zsh-users/zsh-syntax-highlighting/blob/master/INSTALL.md
zsh-users/zsh-syntax-highlighting
